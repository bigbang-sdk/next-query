const canvasProps = {
  canvasWidth: 1350,
  canvasHeight: 500,
};

const gridProps = {
  gridHorizontal: 13,
  gridVertical: 5,
};

const heroTextProps = {
  text: "NEXT QUERY",
  fontSize: 96,
  fontWidthEstimate: 0.61,
  letterSpacing: 12,
};

const tagProps = {
  tagRowWidth: 825,
  tagWidth: 167,
  tagHeight: 40,
  tagRadius: 8,
  tagFontSize: 20,
};

export const CANVAS_DEFAULTS = {
  canvasProps,
  gridProps,
  heroTextProps,
  tagProps,
};
