const DEFAULT_CANVAS_SIZE = 756;
const CANVAS_SIZE = DEFAULT_CANVAS_SIZE;
const SCALE = CANVAS_SIZE / DEFAULT_CANVAS_SIZE;
const CANVAS_WIDTH = CANVAS_SIZE;
const CANVAS_HEIGHT = 380 * SCALE;
const HEADER_HEIGHT = 55 * SCALE;

export const CANVAS_DEFAULTS = {
  DEFAULT_CANVAS_SIZE,
  CANVAS_SIZE,
  SCALE,
  CANVAS_WIDTH,
  CANVAS_HEIGHT,
  HEADER_HEIGHT,
  BODY_HEIGHT: CANVAS_HEIGHT - HEADER_HEIGHT,
  BOX_WIDTH: 160 * SCALE,
  BOX_HEIGHT: 45 * SCALE,
  CIRCLE_ICON_SIZE: {
    BROWSER: 3.8 * SCALE,
    CIRCLE: 4.25 * SCALE,
    BOUNDING_BOX: 20 * SCALE,
  },
};
